<section>
<h1>Write a Comment</h1>
  <%= form_with model: @comments, url: user_post_comments_path, method: :post, local: true, scope: :comment do |form| %>
    <div class = 'form'>
        <%= form.text_area :text %>
        <%= form.submit 'Add a comment' %>
    </div>
  <% end %>
</section>